export const STARTER_WORKBENCH_PROJECT_README_MD = `# Polaris 小工作台

这是小助手家的默认工作区。打开后，你会看到一份可以直接编辑、预览和复制的 Markdown 文档；它也展示了 Polaris 如何把页面、样式、交互逻辑和说明文档放在同一个工作区里维护。

## 适合用它理解什么

- 工作区是一组文件，不是单张卡片。
- 修改 \`index.html\` 会改变页面结构和显示文案。
- 修改 \`styles.css\` 会改变视觉样式。
- 修改 \`app.js\` 会改变文档拼接、任务和摘录的交互。
- 运行工作区时，Polaris 会把本地引用的 CSS 和 JS 一起预览。

## 文件职责

- \`index.html\`：页面结构和用户能看到的文案。
- \`styles.css\`：布局、主题和组件样式。
- \`app.js\`：正文、任务、摘录、主题切换和 Markdown 预览。
- \`README.md\`：这份工作区说明。

## 状态规则

你在页面里写下的正文、任务和摘录会自动保存；下次打开这个工作区，可以从上次的位置继续整理。
`;
